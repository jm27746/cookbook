<h1>This is the view for new recipes. It will have a form to input and Post a new recipe to the DB</h1>

<%= form_for @recipe do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :instructions, size: "60x12" %>
  <%= f.submit "Sumbit" %>

<% end %>
